# Kustomisasi URL

1. URL yang di pendekkan bisa dibuat custom/ tidak random.
2. URL dapat dapat dibuat private/public
    - Jika Public seperti biasa orang yang akses langsung di redirect
    - Jika Private, sebelum redirect perlu input password yang sudah di set